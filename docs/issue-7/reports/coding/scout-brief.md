# Scout brief — issue-7

Skipped. Reason: this is a pure repo-internal grep-and-repoint task with no
open design decision — the spec (issue text) fully determines what to keep
(own record states/format) vs strip (statements naming which role a state
summons). No external category or exemplar applies.
