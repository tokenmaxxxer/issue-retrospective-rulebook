---
name: coding-survey-issue-5
---

# Current-state survey — issue #5

## Write set
- `README.md:55` — replace `muster` with `on-the-record` in the sentence
  "muster installs it per role alongside the core marketplace".

## Out of scope
- All other files (historical docs untouched per issue body).
- No other "muster" occurrences found in README.md (single grep hit).
